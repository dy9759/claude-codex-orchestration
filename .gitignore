# Per-user session state — auto-created on first use, do not commit
.eval-scores.jsonl
.error-log.jsonl
.codex-quality.jsonl

# Per-user sentinels
.orch-plugin-hints-shown
.orch-claude-md-seeded

# Per-plan decision state (regenerated each Plan cycle)
.decisions-pending
.decisions-approved

# Pending escalation queue (not yet uploaded to skill repo) — local-only retry state
.issue-candidates.jsonl

# Long-session /co:eval pressure counters (per-user, per-machine)
.last-eval-dispatch-count
.last-eval-timestamp
.skip-eval-this-session

# Note: .issue-log.jsonl is TRACKED (evolution record, like .skill-scores.jsonl)

# OS / editor cruft
.DS_Store
*.swp
*.swo
*~
